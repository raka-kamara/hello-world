//this program will calculate thre integer
int main(){
    int x, y, z, result;
    printf("Enter three integer:");
    scanf("%d%d%d", &x, &y, &z);
    result = x * y * z;
    printf("Result is:%d", result);
    return 0;

}
